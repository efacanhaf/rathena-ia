// =============================================================================
// DimensionsRO — battle config overrides (Phase 5 — drop & exp rate baseline)
// =============================================================================

// charcommand symbol: stock '#' is intercepted by the hexed DRO Ragexe
// (treated as whisper prefix client-side, never reaches server). '!' goes
// through.
charcommand_symbol: !

// =============================================================================
// Philosophy: mid-rate baseline that leaves headroom for EXP/drop EVENTS
// (weekend/anniversary boosts) without trivializing the grind.
//
// Baseline pace estimates:
//   - Lvl 1-99 (Trans):           ~6-8h     (5x base/job exp)
//   - Lvl 99 -> 175 (3rd class):  ~24-32h
//   - Total to max level:         ~30-40h casual play
//
// Event multipliers (applied via @addrate or NPC) lift this to:
//   - Weekend +5x event:    effective 10x base, ~18h max level
//   - Anniversary +10x:     effective 15x base, ~12h max level
//
// Cards: 100x normal/boss (~1%), 10x MVP (~0.1%). Eduardo's target rate.
// =============================================================================

// === EXP ===
// Base 10x (era 5x — dobrado em 2026-05-02)
base_exp_rate: 1000
job_exp_rate: 1000
quest_exp_rate: 600

// Multi-level up: permite subir varios niveis num unico kill (sem cap em 99%).
multi_level_up: yes
multi_level_up_base: 0
multi_level_up_job: 0

// === Common items (potions, materials) ===
item_rate_common: 1500
item_rate_common_boss: 1500
item_rate_common_mvp: 2000

// === Heal / Use (consumables) ===
item_rate_heal: 1500
item_rate_heal_boss: 1500
item_rate_heal_mvp: 2000
item_rate_use: 1500
item_rate_use_boss: 1500
item_rate_use_mvp: 2000

// === Equip (weapons, armor, accessories) — 20x normal, 10x mvp ===
item_rate_equip: 2000
item_rate_equip_boss: 1500
item_rate_equip_mvp: 1000

// === Cards ===
item_rate_card: 10000        // multiplier 100x regular (garante clamp ao ceiling 1%)
item_rate_card_boss: 100    // multiplier 1x mini
item_rate_card_mvp: 100     // multiplier 1x MVP
item_drop_card_min: 10     // 1% floor para TODOS cards
item_drop_card_max: 100     // 1% ceiling para TODOS cards

// === MVP-specific drops (Old Card Album, Old Violet Box, etc) ===
item_rate_mvp: 300

// === Treasure (WoE — secondary scope) ===
item_rate_treasure: 300

// === Add drops (script-injected) ===
item_rate_adddrop: 500

// === @whodrops / @mi — mostrar rate BASE (sem level penalty no display) ===
// Default 1 aplica pc_level_penalty_mod no display, fazendo o GM achar que
// drop ta zoado quando na verdade e penalty pelo lvl do char vs mob.
atcommand_mobinfo_type: 0

// === Stat caps — Ep 17.1 server (no 4th classes) ===
// iRO oficial 3rd class cap = 120. rAthena default 130 assume 4th class progression.
// Travado em 120 pra match experiência canônica 17.1.
max_third_parameter: 120
max_third_trans_parameter: 120

// === Dynamic NPC ===
// Faz NPCs criados via duplicate_dynamic (ex: Booster Call) ja
// nascerem virados pro player. Default rAthena = no.
feature.dynamicnpc_direction: yes
